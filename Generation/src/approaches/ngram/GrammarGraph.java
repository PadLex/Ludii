package approaches.ngram;

import compiler.Compiler;
import game.Game;
import main.grammar.Description;
import main.grammar.GrammarRule;
import main.grammar.Report;
import main.options.UserSelections;
import parser.Parser;

import java.util.ArrayList;
import java.util.List;

public class GrammarGraph {

    public GrammarGraph(List<GrammarRule> rules) {

    }

    class Node {

        Node () {
        }
    }
    public static void main(String[] args) {
        //Grammar.grammar().generate();
        //System.out.println(Grammar.grammar().getRules());


        //GrammarGraph graph = new GrammarGraph(Grammar.grammar().getRules());

    }
}
